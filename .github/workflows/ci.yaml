name: specs-workflow

on:
    pull_request:
        branches:
        - '*'
    push:
        branches:
        - master
        - main
    workflow_dispatch:


jobs:
    specs:
    #     name: microvm-kernel-${{ matrix.label }}
    #     runs-on: [actuated-arm64-4cpu-8gb, "${{ matrix.label }}" ]
    #     strategy:
    #       matrix:
    #         label: [a102,ec2]

        name: specs
        runs-on: [actuated-1cpu-2gb,do]
        steps:
        - uses: actions/checkout@v5
        - name: Run script
          run: |
              ./specs.sh
    

name: specs-workflow

on:
    pull_request:
        branches:
        - '*'
    push:
        branches:
        - master
        - main
    workflow_dispatch:


jobs:
    specs:
        name: specs
        runs-on: [actuated-1cpu-4gb, do]
        steps:
        - uses: actions/checkout@v5
        - name: Run script
          run: |
              ./specs.sh

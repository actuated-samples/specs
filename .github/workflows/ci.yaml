name: specs-workflow

on:
    pull_request:
        branches:
        - '*'
    push:
        branches:
        - master
        - main
    workflow_dispatch:

jobs:
    specs:
        # strategy:
          # matrix:
             # os:
             # - actuated-4cpu-4gb
             # - actuated-arm64-4cpu-4gb
             # - ubuntu-latest

        # name: specs-${{ matrix.os }}
        # runs-on: ${{ matrix.os }}
        name: specs
        runs-on: [actuated-arm64-8cpu-12gb]

        steps:
# vmmeter start
        - uses: alexellis/arkade-get@master
        - name: Install vmmeter
          run: |
            sudo -E arkade oci extract ghcr.io/openfaasltd/vmmeter:latest --path /usr/local/bin/
        - uses: self-actuated/vmmeter-action@master
# vmmeter end

        - uses: actions/checkout@v1

        - name: Check specs
          run: |
            ./specs.sh
